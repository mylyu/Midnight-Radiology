# 全书剧情总线 v3.3 · 发布交接

日期：2026-09-25。作者明确要求“修订全书剧情总线，推送GitHub、Pages”。

## 范围与基点

- 本轮分支：`codex/story-bus-v3-3-release`，起点`30e8692`；开始时远端`origin/main`是`5a2b494`。
- 文档按现有代码修订，不为迁就旧总线修改游戏。第一章、第二章、DR/DSA运行源码、媒体、依赖与部署工作流均与`30e8692`保持一致。
- 此次发布授权覆盖`5a2b494..30e8692`九笔已完成本地提交：数值/防误触修复、无声反馈与轻量成长、剧情微调、主任白班语音、厚薄层问答、删除腹痛/细微腕伤、CTA及人物修订、对应测试、第二章姓名学号凭证。历史HANDOFF“本地未发布”是当时状态，由本轮明确授权接续，不是此前已偷偷上线。

## 总线改动

1. 将第二章旧“五夜＋验收/热量槽/IR救场”设想替换成实际三夜两白、当前病例、十四次三秒采集与两次处理演出。
2. 区分进口64排旧CT、国产128排新CT、蒙布老CR；第一章第三方球管后果在CR上回响。
3. 补齐十五根针、离线盒子、日出、教具/双钥匙、跨章礼物、专家握手、陆舟低剂量电话及尾声凭证。
4. 修正文档误记：男女主角现版同数值开局，DR/DSA只写实际可玩机制；神秘患者老年形象、未锁具体来院日；第二章并非所有风险节点30%。
5. 第三至五章与未做DLC明确标规划，保留1998长线。DR聚餐相对日期、未来MRI场强和宝宝月龄等记为待后续统一，不擅改已发布玩法。
6. 旧v3.2评审和旧差异表保留历史，不作为自动补回剧情的待办。

## 测试保护原则

旧保护测试曾将总线全文视为冻结配置。此次用户明确授权修该文件，因此增加独立单路径文档投影：`app/tests/story-bus-revision.mjs`及`docs/story-bus-v3-3-review.json`绑定旧、新全文SHA与`30e8692`基点，只对总线这个路径认可精确替换。旧审计ledger/pin不改，不泛化放宽任何运行文件；额外断言本轮生产源码、媒体、配置与工作流没有差异。

## 发布前验证

- `npm run build`：类型与生产构建通过，`index-CCAaJmXY.js` / `index-gs3iXliY.css`；200逻辑图片23.23MiB，保留原大包警告。
- `npm run lint`：原10 errors / 2 warnings，没有以改规则掩盖。未在本轮升级依赖；此前记录的依赖审计风险仍待独立处理。
- 文档精确gate、凭证/CTA独立保护、第一章30共享函数/263旧素材保护通过；数值打磨、素材交付、连续切片、回礼、图片、打卡、同事支线等历史链通过，原baseline/ledger不改。第二章现役15勋章/17卡片完整性通过。
- 历史`ch2-data`漏登记上轮两个获批桥接（夹层三维、晨会CT感叹），此次仅绑定旧CTA固定审核身份补这两项精确适配；原`5fea950`基点、奖励/任务断言全保留，复跑通过。没有用放宽断言掩盖运行回归。
- 凭证纯函数17组及本地生产浏览器7夹具通过；CTA呈现13生产夹具通过；经营页桌面/390px布局通过。证书刷新、旧档、双击、教师入口、断层/三维不重复、立绘/远程呈现均保留原断言。截图已检查，凭证测试0页面错误/0素材失败。
- 浏览器证据：仓库旁`story-bus-release-review/certificate/`、`cta/`、`layout/`；测试用独立临时存档，不触及作者浏览器存档。本次为定向发布回归，不宣称又完整通关两章。
- Git普通推送dry-run成功，远端仍为`5a2b494`且是本分支祖先；后续正常快进发布，不强推。实际部署回执完成后补记。

## 正式发布回执

- `a2be5ec`已普通快进到`origin/main`，同时保存远端`codex/story-bus-v3-3-release`分支，未覆盖历史。远程仓库总线已核对为v3.3。
- [Pages工作流36104466019](https://github.com/mylyu/Midnight-Radiology/actions/runs/36104466019)的build/deploy均成功，head为`a2be5ec4e2e43ca3e669245ffea0573b65d21482`。
- 正式地址：[深夜影像科](https://mylyu.github.io/Midnight-Radiology/)。直接访问根地址已返回`index-4_wVb7hl.js` / `index-gs3iXliY.css`，不依赖版本参数。
- 远端JS与本机文件名不同的原因已查明：现有Kimi检视插件的`code-path`在Linux使用`/`、Windows使用`\\`。仅统一这些注记路径后，两个JS全文逐字符一致；不是丢提交或代码不同。CSS、主任白班/罗阿姨两条音频、腕部细裂图、夹层三维图、白班背景均HTTP200且SHA与本地交付字节一致。
- 正式Pages原凭证浏览器7夹具全通过，桌面/390px、刷新/旧档/双击/两个教师入口，0页面错误/0素材HTTP失败。首次运行在印章下载尚未完成时立即断言，已修测试驱动为最多20秒等待加载成功后仍执行原完整断言；不改游戏、不删断言。初次失败证据与复跑报告均保留在`story-bus-release-review/live-certificate/`。
- 本回执与上述测试时序修正为后续文档/测试提交，生产代码仍保持`30e8692`；再次main推送会运行同一Pages工作流并发布同一游戏资源。

## 回退与交替协作

- 仅撤本轮文档：在本分支日志确定“sync story bus”提交并`git revert`，不要hard reset或强推。
- 撤已发布的某个游戏改动：按对应功能文档和提交依赖逆序revert；不能仅撤总线就假定撤了运行代码。`30e8692`保存文档修订前的最新游戏，`5a2b494`保存此次发布前线上基点。
- 不清玩家存档；本轮不产生新的进度迁移。Kimi/其他AI接手先读v3.3和HANDOFF顶部，再读对应轮记录，保留ImageCAS商用前替换/另授权限制。
- Pages用既有main自动部署；不更改自定义域名、权限或缓存方案。正式地址无需永久追加`?v=`。
