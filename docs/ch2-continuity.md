# 第二章跨章承接修正 · 2026-09-20

## 范围与回退

- 基点：`17d3157`（DeepSeek QA）；工作分支：`codex/ch2-story-continuity`。
- 本轮按“先时间/物件、再角色记忆、再登记与开柜回报”实施，仅本地，不推送部署。不合并 `codex/ch2-rework`。
- 保留三个夜班、两个白班、晨会、自由行动、小卖部、徽章、调窗、夹层与金属病例、儿童复查；不新增对话按钮或知识小游戏。
- 第一章对白冻结，`data.ts`只改第二章使用的陆舟称谓（研究生同学→本科室友）。第一章、DR、DSA剧情/音频文件不改。总线v3.2原文不修改。
- 在干净工作树从 `17d3157` 新建对照分支即可对比；撤回本轮用 `git revert <本轮提交>`，不要 hard reset。浏览器存档须另备份，Git回退不回退玩家进度。
- 用户实际浏览器与存档未操作。诊断测试使用隔离fixture；独立连续试玩使用另外的真实新存档，两类证据不能混称。

## 剧情变更定位

| 范围/节点 | 变更与边界 |
| --- | --- |
| `c2n1_3/6`、`ab*/an*`、新增 `c2n1_old_ct` | 交付延期交代；旧CT烧管→待配件→拆机处置→新CT接替。CR球管后果保留，与CT分开；普通片由夜间开放的DR接班。照片是拆机前留影，不是把整台CT摆走廊。 |
| `c2n1_s2`、`c2d2_reg0…6`、`c2d2_t0`、`c2d4_reg*` | 2024登记缺页得到追查与整改。老周承认撕页；主任承担后续程序责任。按 `n5_cover` 区分亲历发言，缺失旧flag不强安参与经历。临时号、报告跟踪、后补手续与救助对接在后续病人中落地。不是免费拍片=骗保，也不是新一套手续考试。 |
| `ch2StepForState` | 纯文本读取 `mystery_told`、第一章完成、`mystery_asked`、`fixer`、`wen_card`、`kai_friend`、`archive_sealed`；没保存的选择不伪造。女性陆舟人称修正，不更换声轨。 |
| `c2n3_d2/k2/x*` | 不提前声称已看到“五年换科”；移除突然冒出的1998缴费单；承接父亲旧片与患者自己的头痛。没有找到父亲、没有把流浪老人等同父亲。具体11月日期仍待作者统一，未自行编造。 |
| `c2n5_0…5/a*/k*` | 明确两半钥匙来源与交还，黄铜小柜钥匙是另一件物品；开柜不耗AP、不被0AP卡住。改为封柜前教学片、笔记、1997合影；年轻老周可认，主线不再锁在付费钥匙后。购买钥匙只多看小铁柜借阅卡。撤除“1999年11月片袋早于1999年1月封柜”“1983刮脸照片”等冲突。 |
| `CH2_EVIDENCE` | 第二章照片改独立ID `ch2_team_photo`，不再覆盖第一章 `EVIDENCE.old_photo`。旧节点和历史flags不删除；旧证物内容按本轮剧情纠偏，不能把两张照片重新合并。 |
| `c2n5_g*`、`c2am*` | 本章是夜班主值交接，返聘带教与备班仍在，不提前演2028正式离岗。牛肉/杯子保留生活段，结尾留“周老师”认可；省院预告明确2028，去掉“半年后”。完结页同步，不再称剧情仅一周。 |
| `c2d4_e*`、`c2n5_e*/n7/n8*` | 会议讨论已安装终端的服务升级与整改；交代质控样本经控制台导入终端。三种选择都不能继续外传敏感样本，完整断网审计仍有单独后续。断网后查留存日志，不再抓一周实时包。大事记不把所有选择记成断网审计。 |
| `c2n5_ring_trigger/ring_scan/r0/r3/g0` | 开班日检正常，后续报警触发体模复核；停机后分流交班，不无说明继续临床扫描。水箱来自科里，陆舟留下的是记录，不是带走后又凭空回来的体模。 |

## 画面与节奏

- 新增13处明确“采集→出图”的节点连接；结果节点不再同时启动曝光声。去掉首例上床前的血肿图、肺患者进门即出图、旧片还没调出来就显示等提前揭图。
- 调窗目标、原有病例选择/奖励不动。体模讨论不再指着单张图说不存在的左右双图。
- 咖啡、奶茶、零食、卤牛肉、杯子取消剧情中央大图；商店图标不改。患者/人物出场、已批准配音与音量设定不改。
- 新增三张像素证物图（原图全部保留）：`ev_ch2_teaching_archive.png`、`ev_ch2_team_1997.png`、`ev_old_ct_retired.png`。用内置imagegen，提示词/来源/哈希见 `ch2-continuity-assets.json`。都是虚构叙事素材。
- 不以大段说理解释新制度；保留小唐催开诊、牛肉被吃、茶杯和洗饭盒等松弛段落。紧张处仍是急诊、影像伪影、数据发现和夜班故障。

## 验证记录（初轮）

- 类型检查与生产构建通过；lint仍为已知10 errors/2 warnings，不声称lint全绿。
- `ch2-data.mjs`：保留所有旧节点、奖励和调窗参数；仅明确allowlist允许新采集连接、登记连接、开柜AP及大事记修正，非整体放宽断言。
- `ch2-grants.mjs`：增加从起点可达性检查，徽章引擎发放用AST检查真实数组赋值，不再用“源码里出现字符串”充当可获得证据。
- `ch2-continuity.mjs`通过：记忆分支、证物ID、日期、0AP开柜、13组采集结果前驱与资源存在性。
- `ch2-continuity-browser.mjs`通过：13组实际DOM出图顺序、0AP开柜桌面/手机、新增三图桌面/手机、6组历史选择与刷新。
- 现有 `ch2-visuals`（21人物、五档旧书、六场景）、`ch2-collection`通过。
- `FULL_WALK=1 ch2-flow`与两轮独立第一章→第二章试玩正在进行；最终结果和发现另补，不能把此初轮文档当全部验收通过。

## 独立试玩期间的已确认修复（第二笔提交）

- 两位独立玩家均在第一章第1夜 `n1_end` 遇到空对白硬阻塞，回标题续玩也不恢复。开发环境 React StrictMode 清理了600ms结算定时器，但旧实现提前把 `finishFired` 置为true，第二次setup不再安排回调。现仅在回调真正执行时置位；不改第一章对白、奖励、UI或扫描读取。两位玩家已在各自原存档恢复到白天，未跳章。
- 新增 `tests/ch1-settlement.mjs`，隔离存档下验证第一章五个end节点自动结算且只结算一次，防止“修空框”变成重复发奖。开发StrictMode实测通过。
- 第二章卒中段 `c2n3_m11` 已宣布给药却标47分钟，下一句又称52分钟给药；角标统一52，奖励和事件不变。
- 冠脉讨论 `c2n3_h1/h3*` 精简为当前病例由团队评估，删除“胸痛不是肺/没有第三条路”和CTA必然先手的绝对化。风险用“中危继续评估”，不是所有低危都需CTA；不增加分层考题。参考：[ACC对2021胸痛指南的官方摘要](https://www.acc.org/latest-in-cardiology/ten-points-to-remember/2021/10/27/14/06/2021-guideline-for-chest-pain-gl_chestpain)（中危可选解剖或功能检查；低危可能无需额外急诊检查）。
- 最新 `ch2-data` 为148/320对比早期基点的文案差异（包含前几轮，并非本轮改了148句）；类型/构建、连续五班249节点通过。完整独立双轮尚在继续。

## 画面返回与记录同步（后续局部修正）

- `c2n1/c2n3/c2n5_hub` 明确返回CT控制室背景，防止从旧片库/走廊探索回来后整段沿用上一地点。13个新增采集节点全部指定对应白天/夜间控制室，首例上床后也明确切到控制室。
- 新三图的浏览器截图fixture使用真实剧情背景（旧片库/走廊），不以控制室底图代替。手机/桌面及13组出图顺序复测通过。
- 开柜末两句收束为陆舟当下体模记录与老周笔记上的改错，不把1997笔记当现设备质控记录，也不由角色替作者解释哪些谜底不能揭晓。
- 大事记同步新CT处置、陆舟所有分支的共同事实、卒中上级取栓衔接、神秘病人仍需随诊。不再把仅某分支说过的话写成所有玩家的回忆。
- 合影条目移除面向开发者的“旧存档记录保留”说明（兼容逻辑和文档仍保留）；未解锁章节卡同步“启用初期”，不再残留“第一周”。

## 独立B轮暴露的局部错误

- 白班队列将同一名腹痛小伙重复列为门诊/急诊，扫描后仍留在队列。现在区分肺复查大爷、候诊门诊大爷、急诊腹痛小伙、腕伤学生、术后加急及创伤；检查结束移出，创伤有到达/出图状态。原三选项及其奖励不变，只有两个队列显示flag记录该次实际先检查谁；每次进入选择结果重置另一flag，刷新可恢复，不新增候诊小游戏。
- 旧存档停在创伤阶段但没有新flag时，不推断早先到底先查了谁；采用未确认进度的保守列表。重新从相应选择结果节点继续即可得到完整状态。
- 旧书第2页把骨窗/肺窗看腹部写成“死白/漆黑”，方向错误。改为宽骨窗软组织灰阶差异小、低窗位肺窗软组织偏亮。按现有线性显示式，40HU在4000/250为约0.45，在1500/-500为约0.86；此计算只用于回归，不加给玩家。
- B轮截图确认日间背景的CT外壳敞开，主任务再查夜间背景也如此。用内置imagegen分别局部修图，关闭外壳、移走拆盖和工具箱，新增 `bg_ctcontrol_day_ready.png` / `bg_ctcontrol_ready.png`。只在第二章解析旧背景键时映射，老存档有效，原图及其他章节不改。提示词/源文件/哈希见 `ch2-ready-backgrounds.json`。
- 第4班病例没有新操作/选择，是用户此前明确否决新互动、要求夹层与金属两个病例后的保留设计。独立玩家对节奏/主角口吻的批评原样入档，不因此恢复清单/考试或偷偷重写病例。
- 只换两组显著让主角“装外行”的问答：重组是否再扫→保留原始薄层和重组；脑内裂缝→调窗能否处理金属条纹。原节点、图、分支、奖励不变。
- 运动伪影选项删去无根据的“只多30秒”承诺，仍保持固定DNT演出与必要范围补扫。开柜原本就是必做主线，现按钮明确“交接主线·不耗行动力”、未完成交接时不伪装成可直接开诊；不把柜子答案改成新阴谋，封存原因留待作者与前传统一。

## 留给协作者的边界

- 不恢复六格核对小游戏，不改为医护岗位流程课；不要恢复“显示全文/继续”按钮或剧情内AI来源图注。
- 不合并寻父/流浪老人/老周三条线；1998全貌、父亲身份和正式离岗的答案不在本章。
- 已批准的新病例和“三夜两白”优先，不照总线旧排班机械回改。
- 第一章照片原条目沿用其既有解锁flag（`archive_sealed`），该老问题未趁机重写第一章。
- 第一章音频/既有教学简化若在独立测试暴露，先单列证据，不以“顺便优化”大规模回改。
