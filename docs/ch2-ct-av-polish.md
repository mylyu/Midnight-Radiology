# 第二章视听与经营页反馈修订 · 2026-09-24

基点 `42d18ce`；分支 `codex/ch2-ct-av-polish`。本轮是作者对完整循环的定向反馈，不改第一章、DR、DSA及已确认角色声音，不推送或部署。

## 最新补充：章节名称与强制播放

作者随后要求“每日结算画面的所有章节名称核对；扫描动画不可以跳过”。此次以 `e8d04ef` 为基点，独立提交 `fix(ch2): align chapter labels and require scan playback`，覆盖下文历史的“可跳过演出”规则。

| 设备称呼 | 经营页章名/状态依据 |
| --- | --- |
| 老伙计（CR · X光机） | 第一章「老伙计」，按存档显示 |
| CT 扫描仪（国产128排） | 第二章「快与狠」，当前章 |
| 二手超声 | 第三章「回声」，超声科跟班，未开放 |
| 3.0T 磁共振 | 第四章「共振」，未开放 |
| PET/CT（新增规划行） | 第五章「微光」，省院进修，未开放 |
| 楼上 DR 机房 | 注册表中的「番外篇 · DR 白班」，按实际开放和存档状态 |
| 介入室 C型臂DSA | 注册表中的「番外篇 · DSA 导管室」，按实际开放和存档状态 |

设备称呼逐项对照第一章 `DayScreen`，正式章名来自总线时间表，不改第一章和总线。增加第五章只补规划展示，不新增入口或改变解锁逻辑。

采集3秒、重建1.5秒均必须播完；删除跳过按钮和 `onSkip` 接口，`App.tsx` 只改第二章组件调用处。保留静音开关与自动时钟推进。普通点击、Escape/Enter/Space不提前结束；静音只改变声音。刷新/后台恢复按已存时间继续，不依赖声音结束事件。

回退本次要求仅revert该独立提交即可恢复 `e8d04ef` 的名称和可跳过行为；此前真实音频、腰椎骨盆病例与无明细账目的经营页仍保留。

本次实测：名称/设备映射与桌面390px布局通过；扫描键盘/背景/按钮无提前推进，完整3秒及1.5秒、自动恢复、静音和焦点限制通过。完整五班331轮/278剧情节点、17处真实等待、12观察/腕窗、晨会五题/尾声、94笔流水与12病例对账通过，第一章/DR/DSA状态不变。类型/构建/定向lint通过，全仓lint仍既有10 errors/2 warnings；30保护函数/263媒体冻结通过。记录目录为仓库旁 `ch2-no-skip-flow-review/`、`ch2-unskippable-scan-review/`、`ch2-chapter-names-review/`，不覆盖上一轮记录。

## 最新行为

1. **采集3秒，原视频声音。** 15个采集入口统一3000ms；取作者提供视频81.000–84.000秒原声。仅0.7增益、25ms淡入/80ms淡出、24kHz单声道128kbps MP3；运行时增益0.24。无变调、加速、合成叠音、循环或额外结束嘟声。两处同数据重建仍1500ms且静音。动画时钟独立于音频，拒播、缺图、切后台、恢复不会等待音频或锁死。跳过仅跳演出。
2. **具象像素检查室。** 新图显示完整CT机架、检查床、患者与房间；替换抽象几何机架，用轻微镜头变化及阶段反馈呈现过程。不是把射线画成可见光束。图像生成采用imagegen与原控制室风格参考，不覆盖旧图。
3. **腰椎／骨盆车祸病例。** 腰胯撞车门、移动疼痛，由医师评估并申请CT；保留临时身份回响。新配图为骨窗腰椎矢状位／骨盆冠状位，讨论同次数据多方向重组及完整序列交接，不凭两张图确诊骨折。原节点、分支、奖励、队列、声音和观察ID不变，病例记录名称同步。
4. **经营页向第一章对齐。** 移除明细账目/流水/历史切换控件，显示设备与章节状态。CR依据第一章进度；CT本章；超声/MRI未开放；DR/DSA读取实际注册与存档状态。保留四项当前数值、本班净变化、背包、收藏与底部功能。仍须明确点击才进下一班；没有CT耐久或维修费。

存档中的账本与去重数据继续保留，但不向玩家显示逐笔账目。已有观察ID不改名，避免旧存档再次拿奖励。原图片和已退役合成音效文件仍保留以便回退。

## 素材与限制

- [图像完整提示词、生成文件、哈希及审核限制](ch2-ct-av-assets.json)。生成腰椎首稿有重复椎体和杂字，未接入；修订后接入。图片是教学插图，不是临床DICOM或可靠HU数据，未宣称医师独立审核。仅承担骨窗／重组／完整序列教学，不设置虚构骨折定位答案。
- [音频来源、SHA256与完整ffmpeg参数](ch2-real-ct-audio.json)；可复现脚本 `app/scripts/extract-ch2-real-ct.mjs`（接收原视频路径）。原视频留在作者目录，未复制进仓库。
- 检查过81秒视频帧、音频解码/时长/电平、HTTP返回文件哈希和浏览器Web Audio解码；选段自动转写为空。**当前工具不能听音，自动检查不等于人工试听。**
- 录音来自作者提供的本地视频，画面有Bilibili上传者标记；原录音再分发许可未独立核实。当前仅本地试玩，公开发布前确认，不将其标注为原创CC音效。
- 腰椎及骨盆公开医学资料与边界见 [素材审核记录附录](ch2-loop-assets.md)，未新增患者检查流程考核。

## 验证

### 当前版本实际执行

- 类型检查、生产构建通过；全仓lint仍为既有10 errors/2 warnings，未新增；Vite原大包体警告未隐藏。
- `ch2-scan-registry.mjs` / `ch2-scan-browser.mjs`：15个3秒采集、2重建、原声文件/来源哈希、单次播放、1.7秒恢复、静音/拒播/跳过/后台及桌面/390px通过。
- `ch2-trauma-revision.mjs`：对比 `42d18ce`，只允许5处明确病例文案/图片变化；全节点边/奖励/队列/语音冻结通过。`ch2-loop-observations.mjs`通过。
- `ch2-loop-review-projection.mjs`、`ch2-loop-projection.mjs`、`ch2-pacing-projection.mjs`通过。新增独立的 `42d18ce` 逆投影层，旧47-hunk记录及原测试断言不放宽。
- 原剧情数据、节奏、同事支线、视觉浏览器测试，以及账本/送礼/观察回执测试通过。
- 结算布局与收藏页通过：桌面完整设备区；390px内容可滚动、底部操作可见；无详细账目入口。DR/DSA展示按实际可用状态而不是写死锁定。
- 最终 `ch2-loop-browser.mjs` 完整走查：331轮推进、278剧情节点、17演出入口、12项观察+手腕调窗、五班、五道实际晨会题和尾声；94笔流水与12份病例记录对账通过，无重复奖励。送礼/原小何夜宵/无物品/缺图求助/答错继续/购物刷新/390px经营页全部通过。
- `ch1-freeze-loop.mjs`：30个原共享/第一章/DR/DSA函数、263份发布版媒体相对 `05889fa` 不变，含最新批准三条第一章语音。最终走查中第一章与DR/DSA存档进度不变。**第一章完整254节点UI回归是上一轮已通过，本轮没有声称重跑。**

原始截图与轨迹不提交仓库：

- `C:/Users/lvmen/Documents/New project/ch2-ct-av-flow-review/`
- `C:/Users/lvmen/Documents/New project/ch2-real-scan-review/`
- `C:/Users/lvmen/Documents/New project/ch2-settlement-equipment-review/`

## 本地试玩及回退

- `http://127.0.0.1:8798/#/ch2`。正常刷新；需要从头体验用第二章重玩，不清全部浏览器存档。
- 本轮独立提交：`refine(ch2): use real CT audio and restore chapter overview`。回退请对该提交执行 `git revert <提交号>`，或从 `42d18ce` 新建对照分支。不要hard reset，不删除用户存档。下次AI先读HANDOFF顶部，不根据旧发布授权自动上线。
