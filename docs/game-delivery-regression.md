# 全游戏轻量交付：实测记录

本轮以 `c4215b0` 为固定参考；实际浏览器为隔离的无头Edge，未使用作者浏览器档案。稳定生产构建在 `http://127.0.0.1:8805/Midnight-Radiology/`。本地Node为24.16，Pages工作流继续使用Node20；导入包另做无Git历史的干净安装构建。

## 正式连续路线

运行 `PLAYWRIGHT_MODULE=<本机Playwright>`、`GAME_URL=<生产子路径>`、`CHAIN_VARIANT=female-curious`、`CHAIN_ALL_BADGES=1`、`CHAIN_OUTPUT=<仓库外目录>`，然后在app执行：

```sh
node --import tsx tests/ch1-ch2-continuous.mjs
```

全新UI开局，不注入通关档、不缩短动画：第一章262节点、6次CR读片、18次探索；第二章414节点、467循环、17次扫描/重建演出、12次观察、5次班后经营、5道考核和尾声、15/15现役勋章。演出/调窗失败分支/观察/答题中刷新、重玩及跨章保护检查均通过。0页面异常、0图片/音频请求失败。此为一条完整路线，不宣称穷举全分支；本路线新送礼计数为0，送礼规则另由历史账本/送礼测试与loop浏览器夹具覆盖。

## 当前交付与浏览器

- 类型检查/生产build通过，实际dist 309文件、28,762,015 B；全仓lint保持已有10 errors/2 warnings，未清空规则或掩盖失败。
- `game-loading-browser.mjs`：桌面1280和390px手机，冷缓存1 Mbps/120ms本地模拟，UI分别2,841/2,817ms、清晰标题背景4,184/4,143ms；197图逐一真实HTTP、SHA256及浏览器解码通过。全部只从当前canonical路径取得，不从历史Git文件替代网络响应。
- 强制延迟背景：同场景48px内嵌预览可见，标题按钮存在，响应恢复后换清晰图。强制三次失败：保留预览和手动重试，第四次真实加载成功，可进入游戏，无永久锁。
- `ch2-slice-sequences-browser.mjs`：37个实际执行的存档夹具＋2个独立真实迟到音频清理检查通过；完整HTTP录音哈希/解码、实际图集验证。真实标签隐藏/原生生命周期冻结2项在本无头环境不受支持，**不计通过**。不声称解决此前暂停的Edge偶发整页卡死。
- DR/DSA的4个固定场景各在1280/390px检查，共8个夹具：canonical图全部成功、无404/页面异常/横向溢出，并有截图。不是DR/DSA全剧情走查；逻辑和声音由冻结检查保护。
- `ch2-loop-browser`、`ch2-mystery-browser`通过；`ch2-ct-motion-browser`在8798开发服务检查桌面/手机床层运动通过，正式连续路线在8805另外实际经历全部17个扫描/重建入口。

## 历史测试如何处理

媒体明确换编码，历史测试继续比较原图身份，但必须先验证真实新实体哈希；原图只用于标注清楚的历史参考解码。每张图的尺寸和alpha逐像素检查；3张调窗PNG字节及RGBA精确。193张有损图不冒充RGB无损，1张小字证物保留可见RGB无损。全部96个正式MP3与12图集字节保持原样。

48项历史静态清单仍从原`image-polish-regression.mjs`三批读取，没有删测试，最终三批各16/16通过（48/48）。首次失败的退役试听引用改为精确退役记录或显式历史参考（正式B语音仍与参考字节比较）；新增保护层导致的负测试错误文字，只增加该层精确错误格式，仍必须拒绝恶意变更。新独立媒体/源码审核清单不能由运行测试自动重写。

LIVE核对131份源码/配置/历史记录、305份实际媒体身份，187个源码破坏探针全部拒绝；额外197个坏素材和3个catalog破坏探针拒绝。逐像素校验265,882,764个alpha值一致。另有pacing媒体桌面/手机20个真实游戏夹具通过。

以下三个更早期浏览器脚本保留了基点就已过期的剧情假设，未改宽断言、不计成功：

- `ch2-covered-cr-browser`要求old_ct直接返回hub，现行先进入old_ct_price采购对话。
- `ch2-continuity-browser`要求冠脉扫描直接跳到h4，现行先经过coronary_slices观察。
- `ch2-flow`仍寻找旧1997合影选项，现行payoffs为模型底座剧情。

这三份只适配了媒体路径，已定位失败为旧夹具而非缺图，正式连续路线按现行剧情通关通过。未通过项目不纳入“全部测试通过”的说法。

## 报告位置和复现

仓库旁 `game-delivery-review/` 保存loading、continuous、slice-browser、dlc-media、历史static三批和编解码对照报告；不将临时截图、原始大图和测试日志塞入发布包。`game-delivery-freeze.md`说明LIVE边界，`game-delivery-assets.json`记录每份新旧素材身份。Kimi ZIP有独立SHA256SUMS，可先校验文件，再按KIMI_IMPORT进行安装构建；缺Git历史的ZIP不能直接运行历史冻结审计。
