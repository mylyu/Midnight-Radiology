# 第二章：蒙布老 CR 背景补图（2026-09-21）

## 作者要求与范围

按作者截图，在「走廊尽头的凹位里，老CR读片器罩着防尘布」这一段把设备画出来。
这是 CR 读片器，不是已拆机移交的老 CT；不借补图修改设备去向、对白或玩法。

- 分支：`codex/ch2-covered-cr-background`；基点：`64cd2f6`。
- 新增 `app/public/assets/bg_corridor_cr_covered.png`，电梯左侧凹位内的灰黄防尘布旧柜式机器，通道留空。沿用源图的冷色夜景、局部暖灯和粗颗粒像素观感。
- 使用内置 image_gen 对原走廊作参考编辑，无 CLI/API 切换，无后处理。完整提示词、生成路径、输入输出 SHA-256 见 `ch2-covered-cr-background.json`。请求 2048×1152，实际返回 1672×941；保留原始返回 PNG，已在游戏响应式背景中确认可用。
- 原 `bg_corridor.png` 不覆盖，避免影响第一章、其他 DLC 和其他走廊场景；新文件名也避免浏览器沿用旧图片 URL。
- 游戏源码只改 `app/src/game/ch2.ts` 的 `c2n1_an1.bg` 一处。后续该段对白沿用这张背景，`c2n1_hub` 原有设置恢复 CT 控制室。
- 老白零件分支 `c2n1_ab*` 仍使用原走廊，本次没有另画封存箱。正文、配音、选择、奖励、行动力规则、书店、存档结构、第一章、DR/DSA、总线完全不改。

## 回归检查

- `npm run build`：类型检查及生产构建通过；仍有既有 >500 kB 大包提示。
- `npm run lint`：既有 10 errors / 2 warnings，未新增；没有借本轮修改这些历史问题。
- `node --import tsx tests/ch2-covered-cr.mjs`：对比基点全文，唯一源码差异必须是这一个背景键；原图/新图哈希、图片尺寸、另一分支及 hub 返回检查通过。
- `ch2-data`、`ch2-continuity`、`ch2-story-echoes`、`ch2-natural-voices`、`ch2-voice-author` 全通过。
- 旧轮次的精确快照测试使用 `ch2-covered-cr-projection.mjs` 显式还原这一处背景，先断言键只能是本轮新旧两值；本轮独立测试强制 live 为新值。没有删掉旧剧情/配音保护断言。
- `node tests/ch2-covered-cr-browser.mjs`：隔离 Edge 桌面 1600×1000 / 手机 390×844，从自由探索菜单正常点击进入、图片解码、完整文字截屏；桌面刷新不重复扣行动力，依次推进 an1 → an2 → an3 → an4 → old_ct → hub、恢复控制室；旧背景入口存档刷新和另一分支保持原图，均通过。测试音频被 stub，不属于配音听感复验。初版测试误把「已有当前节点的恢复档」视作新进节点，导致 AP 预期错误；改为实际从 hub 点击进入后重跑通过，没有改游戏逻辑来迎合测试。
- 已查看桌面/手机截图，确认新增机器可见且没有被对白框遮住。截图在仓库外 `../ch2-covered-cr-review/`。
- 不操作作者浏览器存档；不是全章所有分支走查，也不宣称修复历史 Edge 偶发卡死。

## 预览与回退

本地游戏 `http://127.0.0.1:8798/#/ch2`；第一夜未购买老白球管分支「看看角落里的老伙计」。
新图直达 `http://127.0.0.1:8798/assets/bg_corridor_cr_covered.png`，不需要清存档。
本轮只本地提交，不推送、不部署。用本轮独立提交 `git revert <提交号>` 回退，不 hard reset。

后续协作者不要把这张图全局覆盖到第一章，也不要把该 CR 读片器画成大型 CT 机架。
