# 第二章患者表现减量 · 2026-09-20

用户最新裁定优先于上一轮“每名患者都配短人声”的安排：能走的不用平车，需要辅助的可坐轮椅，不为凑齐素材硬加呻吟/问候。基点 `117c97d`，新分支 `codex/ch2-patient-restraint`，仅本地。

## 调整

- 腹痛小伙恢复已有 `ch2_pixel_pat_gut.png` 弯腰站姿；候诊延误分支恢复原文“蜷在椅子上”，不再改成平车。
- 胸痛患者、术后复查患者各生成一张 **1024×1536透明像素轮椅图**，保持上一轮人物身份、脸、发型与衣服。使用 imagegen 内置编辑，第一章老周仅作风格参考。新图在 `app/public/assets/ch2_patient_chest_wheelchair.png`、`ch2_patient_postop_wheelchair.png`。原生成路径、完整提示词、哈希、透明角验证见 `ch2-patient-restraint-assets.json`。
- 原故事明确叫不醒的坠床老人、偏瘫失语的卒中老人、车祸多发伤、疑似夹层四例保留原来的平车设定。没有为了“能走尽量走”把原重症写成自行入室。普通门诊、腕伤、候诊、原母子等保持已有站姿/坐姿/抱孩子形象，不一律变成轮椅。
- 上一轮11条新增患者声中撤掉9条：`vox_ch2_fall/stroke/lung/postop/chest/aorta/trauma/wrist/denture`。游戏入口与素材试听页均不再调用，非只调低音量。
- 只保留新增的 `vox_ch2_gut`（插队争执前的腹痛抱怨）、`vox_ch2_waiting`（等了五十分钟的大爷催号）。原神秘病人、肾结石、孩子等已有剧情配音不改；复查大爷之前已批准的结果台词“敢情是没看着”保留，撤的是本轮硬加的“上回的片子，我都带来了”。
- 同时去掉为新增声音硬接的6句问候/呻吟文字；术后只留递申请单的短入场动作，腹痛改成弯腰走到门边。不改原剧情节点、选择、奖励、病例、总线或输入逻辑。
- 旧3张床图和9个MP3不再使用，但文件及生成记录保留，便于回退；**不要照旧生成清单重新全部接回去**。

## 文件边界与验证

- `ch2-patients.ts`：入口声音显式可为空；轮椅/站姿映射、撤声清单与少量动作连接文字。14个实际出场患者仍有形象。
- `App.tsx`、`index.css`：轮椅图与实际对话框高度对齐，保持手机/横屏轮子完整，不改对话UI及上一轮输入修复。
- `ch2-patient-preview.html`：同一地址更新，静默患者没有音频播放器，仅保留2个播放器，不碰存档。
- `ch2-patients.mjs`：检查14出场×3视口、4平车/2轮椅、静默9入口不发人声、原配音/保留2条只播一次；遍历解析后的所有剧情节点确保9个撤声ID无调用，检查预览页仅2播放器及无存档写入。
- `ch2-visuals.mjs`：腹痛的预期恢复为旧立绘，保留原21角色、旧书解锁、病例配图的检查。
- 类型/生产构建通过，保留已有单包>500kB提示；不以静态/浏览器断言代替人工语音回听。本轮没有新生成音频。
- 本轮实测：`EDGE_TEST=1 ch2-patients`、`ch2-data`、`ch2-grants`、`ch2-visuals` 均通过；主代理查看轮椅患者手机截图及更新后的素材画廊，轮椅未被对话框遮挡。lint仍为既有10 errors/2 warnings，未称全部通过；未重跑全剧情249节点通关。
- 原 Edge 随机卡死仍沿用上一轮未完全定位的结论，本轮不以减素材宣称修好了卡死。

## 试玩与回退

素材页：`http://127.0.0.1:8798/ch2-patient-preview.html`；游戏：`http://127.0.0.1:8798/#/ch2`。刷新加载最新版即可，无需清存档。

当前分支相对 `117c97d` 的提交仅包含本轮减量，使用 `git revert` 可单独回退，不用 hard reset。第一章、DR/DSA、总线、原配音文件和存档都没有清理或覆盖，未推送/部署。
