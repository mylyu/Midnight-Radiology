# 旧片库入口与第二章老CR分支修复

基点 `59eeeac`；分支 `codex/archive-corridor-fix`。用户反馈第一章旧片库入口不见、第二章老范说“才三天，就坏了”且背景没有旧机器。只修相关入口与画面，不重配声音、不改已批准的前章选择后果。

## 排查结论

- 第三夜首次旧片库入口条件仍是 `ap >= 1 && !n3_arc`，原始定义没有被最近更新删除。有行动力的新档已实际点击进入 `n3_arc0`。
- 确认缺陷：点击进入就记 `n3_arc`；提前离开尚未找片时入口已隐藏。先找胶片则原剧情立即回hub，有钥匙也不能当夜再开柜；第五夜虽有条件补看入口，不能解决第三夜未发现胶片而整个链路错过的问题。
- 行动力为0也是入口隐藏的原因，与已访问标记不同。没有读取作者本人存档，不能断言其存档究竟是哪一种；两种情况分别处理，不伪造其经历或清档。
- 第二章声音“才三天，就坏了”属于 `bai_tube` 为真（第一章买老白球管）；未买对应“这台先留着吧”。两个音轨均为既有批准版本。背景补图当时只接到 `c2n1_an1`，`c2n1_ab1` 一直留普通走廊（见旧 `docs/ch2-covered-cr-background.md`），不是最近丢了图片或缓存串音。

## 实际改动

- `ch1-exploration.ts`只解析第三夜hub：首次仍扣1行动力；到过且胶片未看，或持有钥匙但铁柜未开，可免费返回原片库路径。可先胶片后柜子、先柜子后胶片，也可离开买钥匙再回来。可完成项已清空则隐藏；不恢复所有已完成地点。
- 未进入且行动力不足时，原菜单附一句咖啡提示，不赠送行动力、不把首次访问改免费。
- 第一章UI与选择事务使用同一解析规则；memo保持选项数组身份，避免打字/900ms计时造成反复锁定。原节点、两件物品、奖励、风险概率、第五夜后续均不改。
- 第二章买过球管分支接上现有 `bg_corridor_cr_covered`，入口文案先交代去年半价球管出了问题，再衔接原“才三天”配音；未买分支不改。老CT仍已拆机、走廊是老CR，不把两种设备混写。
- 全部图片、音频、catalog和章节素材清单未改变，无新大素材、无需清缓存/存档。已通关存档不倒退；第三夜开诊前的相关旧档可继续。

## 定点验收（R2，不全章重玩）

- `node --import tsx tests/archive-corridor.mjs`：首次扣点、提前离开、两种发现顺序、后买钥匙、0行动力咖啡恢复、旧档、完成隐藏；第二章两条分支互斥及背景/音轨匹配通过。
- `node --import tsx tests/archive-corridor-browser.mjs`：390px独立Edge测试档，从已访问/0行动力的hub回片库，提前离开→刷新→找胶片→回来开柜→离开→刷新，确认金币+80、医术+1仅一次、卡片取得、行动力不再扣、完成后隐藏；第二章分别带/不带 `bai_tube` 从hub点入，确认蒙布CR图解码、对应原音轨Blob调用和AP只扣1。页面错误/失败资源请求为0。声音调用检查不是重新听觉验收。
- 原 `detail-transactions.mjs` 44组通过；一次生产构建含类型检查通过（原大包警告仍在）；新增文件定向lint与diff检查通过。
- 初次诊断脚本多走一步后在选择页等待普通翻页箭头超时，是脚本推进写错；未将其当游戏卡死，改用明确预期节点的定点测试。没有降低防连击等待或扫描时长。

## 反思与后续约束

此前只看“顺利探索到底”和“未买球管”路线，遗漏了提前退出、物件顺序与另一条前章旗标。这是覆盖对象选错，增加完整通关遍数并不能直接弥补。已在AGENTS补充：访问不等于完成；两个独立物件要检查顺序/退出；有旗标分支要分别核对台词、声音、背景。修复不应把有效分支硬改成用户熟悉的另一条，也不应未经历史比对就声称新发布回归。

作者已选择“先留本地试玩”，本轮只本地提交，不推送、不部署。回退 `git revert <本轮提交>`；不hard reset、不清玩家进度。历史精确哈希保护不在此局部修复中逐层改写；如后续专门运行，应逐条识别本次获批的入口/背景差异。
