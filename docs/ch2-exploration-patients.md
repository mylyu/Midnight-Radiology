# 第二章：探索恢复、患者出场与输入路径加固

日期：2026-09-20。分支 `codex/ch2-exploration-patients`，基点 `a133558`（标签 `ch2-continuity-reviewed-20260920`）。仅本地，不推送/部署。不要合并被否决的 `codex/ch2-rework`。

## 本轮范围与结论

用户指出：开诊前只剩书/商店/开诊；患者缺少一致的像素立绘、重症卧床形象和短语音；Edge 在不同位置随机完全点不动。保留原对话框、点击/▼推进，不加“显示全文/继续”按钮，不重写病例、主线、商店、调窗、收集或第一章。

**已复现的探索错误不等于随机卡死的根因。** 完成后“再玩一遍”沿用旧班次，而清空进度时未清 `c2n*` 一次性访问标记，使地点被 `notFlag` 隐藏。这两点已修复。用户原 Edge 标签页的连接三次失败，没有刷新、清存档或读取其个人浏览器资料；未复现用户那次真实随机卡死，不能宣称根治。

## 逐项更改

| 文件 | 更改及边界 |
| --- | --- |
| `app/src/game/ch2-exploration.ts` | 新轮只重置 CT 轮次/探索标记及 AP，保留第一章关系、金币物品、历史收藏和其他 DLC。一次事件结束后地点保留为免费短回访；AP/物品不足仍能走走，不发首次事件奖励，也不提前讲已离场人物/已开柜故事。三个原夜班 hub 保留原首次事件与开诊节点。白班原本没有这些 hub，不擅加一套白班剧情。 |
| `app/src/App.tsx` | 完成后重玩和重置均调用新轮初始化。Ch2 打字进度按节点存储，移除渲染中重置状态；选项缓冲按布尔条件而非新建数组重置，切回窗口/对话点击可恢复到期选项；防重复点击锁改为 250ms 有限期。音频不作为推进前置条件。患者卧床布局按实际对话高度留出空间；旧对话交互不变。 |
| `app/src/game/types.ts` | 可选 `DlcProgress.appliedSteps` 保存本轮已进入节点，避免刷新重复扣 AP/发奖励。旧存档缺该字段时，仅将已保存当前节点视为已执行；新轮清空。旧键不迁移、不清存档。扩展新语音名类型。 |
| `app/src/game/ch2-patients.ts` | 14 位实际在场患者的出场映射；患者先出现并发声，检查前讨论可与医护同框，采集/结果页不带入平车。新增 8 句短口语、腹痛“椅子”改“平车”；术后段保留原排队解释，大爷已在此前独立出场。手机截图发现再塞一位候诊大爷会被长对话遮挡，故该处只保留本次抵达的术后患者。不改诊断、奖励、选择和主线连接。电话里尚未抵达的病人不凭空画成已经入室。 |
| `app/src/game/ch2.ts` | 通过运行时解析接入两个独立模块和回访节点；旧剧情节点保留。 |
| `app/src/game/store.ts` | 注册新音轨。新患者口语音量 0.32、三段呻吟 0.13；原配音、音乐和原音量均保留。Ch2 哭声/呻吟也走入口人声的手势重试与会话去重，不循环。 |
| `app/src/index.css` | 仅 `[data-ch2-step]` 中平车立绘的尺寸/位置，手机竖横屏适配；第一章布局不动。 |
| `app/ch2-patient-preview.html` | 本地素材画廊与 11 条音频手动试听，不读写存档、不自动播放，单条播放时暂停其余。仅 Vite 开发预览页，未加入生产入口。 |

### 素材

- 新增 9 张 `app/public/assets/ch2_patient_*.png`：坠床老人、卒中老人、胸背痛、车祸伤、胸闷、术后、腹痛小伙共 **7 张卧床**，另有腕伤学生、候诊大爷。平车图 1536×1024，另两张 1024×1536。原像素人物文件全保留，腹痛旧立姿图不再用于该出场。
- 使用 imagegen，以第一章 `char_zhou.png` 为粗颗粒像素风格参考；腹痛小伙还参考其现有面貌/衣服。提示词/原生成路径在 `ch2-patient-images.json`，尺寸、透明度抽样和 SHA-256 在 `ch2-patient-image-qa.json`。胸闷图左上角 alpha=1/255，其余图角为0；并非不透明黑底，没有为过检测篡改原像素。
- 新增 11 条 `app/public/audio/vox_ch2_*.mp3`，由本机 AuK-Flash 生成，24kHz 单声道 128kbps，约1.37–3秒；轻量起音裁切、-20 LUFS参考/-2dBTP限制。肺部大爷和义齿老人沿用已批准叔叔/老爷子的参考音色，其余新角色单独生成。
- 原始 WAV、失败候选和日志在仓库外 `../AuK/outputs/ch2-patient-entrances-20260920/`，不混入发布素材。命令、权重路径、种子、时长、参考/成品哈希记录在 `ch2-patient-audio.json`。三个 `scripts/ch2-patient-*` 脚本支持检查/重生成；手动重生成前先备份现有最终 MP3，避免无意覆盖用户已批准版本。
- 腹痛初稿出现重复词，最终改成“哎哟……肚子疼死了。”并重生成；坠床/车祸最初疑似夹带英文音节，使用同角色参考重生成短呻吟，最终 ASR 无英文。失败 WAV 保留，仅最终11条接入。

## 验证与限制

- `npm run build`（包含类型检查）通过；保留已有单包>500kB提醒。
- `npm run lint`：仍为基线 **10 errors / 2 warnings**，不称全通过。
- `ch2-data`、`ch2-grants`、`ch2-continuity`：通过，原节点/奖励/任务保留，12/15徽章、18/22卡片政策不变，13组先采集后出图。
- `FULL_WALK=1 ch2-flow`：隔离 Chromium 连续五班 **249 节点**通过，包括原调窗、结算、封柜和病例；不是用户原 Edge 标签页。其后只调整腹痛短句、素材检查与兼容测试，不把测试进程中的热更新说成冻结版本全分支验证。
- `EDGE_TEST=1 ch2-input-exploration`：隔离 Edge 复现旧重玩错误后验证修复；3 hubs/反复地点访问/48次书和商店开关/焦点事件恢复；让音频 `play()` 永久 pending，UI仍能推进；6次真实 reload 验证不重复加减 AP/金币；零 AP 地点可访问且不发跳过事件奖励。
- `EDGE_TEST=1 ch2-patients`：14出场×3视口（1280×900、390×844、844×390），图片解码、唯一入口音频调用、不循环、7床位不被对话遮挡、无提前结果图，通过。语音播放调用被 mock，此项不是声音听觉验收。
- `ch2-continuity-browser`、`ch2-collection`：13实际采集/结果转场、零AP开柜、证物、正常背景、晨会、6种跨章记忆、3队列分支及刷新、历史收藏展示通过。
- 最终 `ch2-visuals` 通过：21个旧映射/原文件保留，腹痛场景使用新床图，五档旧书解锁、六张场景图显示正常。`EDGE_TEST=1 ch2-flow`（非 FULL_WALK）也通过：原对话 UI、快速连点、拒播重试、桌面/手机封柜、关书/刷新、患者先于医护、腕图和儿童图。
- 自动音频检查：11条解码、时长、有效信号、无削波、ASR文本见 `ch2-patient-audio-qa.json`。ASR对呻吟和“到/倒”等同音字不可靠。**本环境不能进行人工回听，不能声称音色/表演已经逐条听验**，请通过素材试听页确认。
- 新 `ch2-input-exploration.mjs`、`ch2-patients.mjs` 为隔离浏览器回归；原 `ch2-visuals.mjs` 只将腹痛场景预期改为卧床新图，仍检查所有旧资源与旧书。原 `ch2-flow.mjs` 添加可选 Edge 启动，不改变剧情断言。
- 无新增“嘟嘟”声、考试、小游戏或医学知识段落；未修改总线文档、第一章内容、DR/DSA、现有批准语音。

## 试玩、交接与回退

- 游戏：`http://127.0.0.1:8798/#/ch2`；素材：`http://127.0.0.1:8798/ch2-patient-preview.html`。旧标签可能需要刷新一次加载新版；**不要清站点数据/存档**。本地服务继续保留。
- 当前节点不变，继续存档即可。若希望真正重玩，使用大厅的重玩/重置，这会清本轮探索状态，不清第一章关系或收藏。
- 回退代码：在当前分支使用 `git revert` 回退本轮提交（查 `git log a133558..codex/ch2-exploration-patients`）；或从 `a133558` 新建对照分支。不要 hard reset、删除整个仓库，或覆盖别的 AI 未提交更改。
- `appliedSteps` 是向后兼容可选字段，旧代码忽略它；代码回退不会自动倒回玩家已作出的游戏选择。要比较同一存档，请先手动导出/备份再试玩。
- **仍待用户复验的硬问题：原 Edge 随机整页点不动。** 本轮只证实并处理共享输入路径的风险，尚无原故障现场的调用栈/覆盖层证据。若复发先保留页面现场，不再归咎某一句台词或要求清存档。
