# 恢复第一章式探索规则 · 2026-09-20

分支：`codex/ch2-original-exploration`；基点：`4566be5`。仅本地，不推送或部署。本记录优先于 `ch2-exploration-patients.md` 中的免费回访安排。

## 用户裁定

互动完成的场景入口应消失。参考第一章实际每夜流程，不要擅自新增“随便走走”、重复人物对话或另一套自由探索系统。这轮只撤掉此前自行添加的回访，不重写人物、病例或章节安排。

## 第一章实际走查

使用隔离 Edge 新上下文，从标题界面选择林小满，正常打卡、选对话、消费和接诊，连续完成五夜到“清晨 · 交接班晨会”。没有注入起始进度、写存档、跳夜、模拟音频或操作用户浏览器资料。脚本读取存档定位当前节点，通过原界面的按钮/点击/▼推进；随后查看了剧情记录和探索前后、白天经营界面的截图。

- 本次经过253个不同剧情节点、18个一次性探索事件，五枚夜班印章、四次白天经营过渡；没有进入晨会答题及尾声，不冒称所有分支通关或逐轨听觉验收。
- 每夜：打卡/开场对话 → 3点行动力的开诊前探索 → 主动选择开工 → 病例/八卦/夜间主线 → 结算。前四夜后进入白天经营，再进下一夜；第五夜后到晨会。
- 事件做完即从当夜菜单消失，不变成回访入口。第一夜走廊遇到随机崴脚额外扣AP；前三夜均通过购买咖啡继续未完成的探索。恢复AP并没有恢复已完成的事件。
- 书和小卖部每夜各开关两次，出入不耗AP，第一章书页按夜解锁。第五夜关系回报不耗AP，但领取后同样消失。

| 夜次 | 实际走到的开诊前事件 | 完成后的菜单 |
| --- | --- | --- |
| 1 | 走廊小雷、值班室歇脚、旧门 | 书、小卖部、开工 |
| 2 | 设备科老范、走廊小何、咖啡机 | 书、小卖部、开工 |
| 3 | 急诊准备、旧片库、值班室对讲机 | 书、小卖部、开工 |
| 4 | 信息科小雷、设备科质控、雯雯 | 书、小卖部、开工 |
| 5 | 厂家保养、老白、旧片库；小钱/老范/小雷关系回报 | 书、小卖部、开工 |

走查脚本：`app/tests/ch1-night-loop.mjs`。原始日志和15张截图在仓库旁的 `C:\Users\lvmen\Documents\New project\ch1-night-loop-review\`，不提交截图或测试存档。随机遭遇和实际路线可能使后续运行节点数不同。此脚本没有修改第一章游戏代码。

## 本轮最小改动

- `app/src/game/ch2.ts`：去掉探索选项改写和11个新增回访节点的注册。原hub条件、原剧情节点/文字/分支/奖励不改。恢复原来的 `notFlag`、AP与物品条件。
- `app/src/game/ch2-exploration.ts`：删掉回访台词和条件绕过。保留 `restartCh2` 原样，避免新一轮继承上轮探索标记；增加明确的旧回访ID→当夜hub兼容映射，不把它们当剧情节点。
- `app/src/App.tsx`：兼容正在回访中的旧存档及本地热更新，接回当夜hub，不重播开场，不清已完成标记、卡片、徽章或其他章节进度。
- `app/tests/ch2-input-exploration.mjs`：撤掉“零AP也应有回访”的错误目标；修正旧静态测试把 `condOk(state, condition)` 两参数写反的问题。新断言确实检查原条件。
- 保留第二章第1/3/5夜原探索、第2/4日原白班、五夜书页进度、商店、病例和既有免费开柜交接条件；没有增设白班hub或照搬第一章的剧情。
- 患者减量、9条新增声音停用、重玩清理、刷新奖励去重、输入加固等独立修改保留。第一章/DR/DSA/总线/图片/音频/CSS没有修改。

## 验证

- `ch1-night-loop`：上述五夜新档UI走查通过，浏览器无页面异常。
- `ch2-input-exploration`：24组hub条件、11个旧ID分别在已完成/未完成存档中的22次接续、4条原事件实际完成/刷新/买咖啡、零AP及物品限制、3个hub全部完成后仅留书/商店/开诊、48次书店叠层开关和焦点恢复、音频永久等待时仍可操作，全部通过。
- 原事件实走包含第一夜走廊、第三夜八卦、第三夜免费二手书事件、第五夜设备巡检。其他一次性条件由静态矩阵和全完成存档覆盖，不冒称本轮逐一走完所有第二章分支。
- `npm run build`（含TypeScript）、`ch2-data`、`ch2-grants`、`ch2-continuity`通过。`npm run lint`仍为既有10 errors / 2 warnings，不是全绿；构建仍有原大包体积警告。
- `ch2-patients`：14名患者×3视口、4平车/2轮椅、9条新增音停用、保留2条新增音及预览一致性通过。
- `ch2-continuity-browser`：13组采集/出图顺序、零AP开柜、桌面/手机证物与晨会、6条前章记忆、3种队列和刷新恢复通过。
- `ch2-flow`：原对话界面、语音拒绝后重试、快速点击、桌面/手机开柜、关书/刷新、患者先于医生、腕部无多余提示及儿童复查图通过。本轮未设置 `FULL_WALK=1` 重走第二章全部主路径。
- 第二章探索截图在仓库旁 `ch2-original-exploration-review/`，已查看完成后的三个hub和咖啡后不恢复已完成入口的截图。

复跑时在 `app/` 中使用 `node --experimental-strip-types tests/<上述脚本>.mjs`。Playwright模块由 `PLAYWRIGHT_MODULE` 指定，支持Edge的脚本使用 `EDGE_TEST=1`；第一章脚本固定隔离Edge，其他脚本可用 `CHROME_PATH` 指向已安装测试浏览器。预览默认 `http://127.0.0.1:8798/`，可由 `GAME_URL` 改写。

原用户 Edge 连接依然不可用，本轮没有操作其标签页或清除其存档。隔离环境可操作不等于证明用户随机卡死已根治。

## 后续AI与回退

不要恢复“互动后随便走走”或为了菜单看着热闹清空当前轮探索标记。菜单只剩书/商店/开诊，在当夜事件完成或AP不足时是正常状态；只有真正重玩时才清本轮标记。

在干净工作树中可从基点 `4566be5` 新建对照分支；在当前分支使用本轮提交的 `git revert <commit>` 撤回，不使用hard reset。Git回退不恢复浏览器存档，旧存档已兼容，无需用户重玩。未来开发先读 `HANDOFF.md` 顶部，患者减量的裁定同时有效。
