# 小凯入场配音 v4：低声私语（2026-09-21）

## 作者反馈与边界

作者认为 v3 句尾上扬、不像悄悄话。台词仍为“跟你说个事儿。”，只调整第二章 `c2n1_b1.sfx`；不动正文、流程、图片、其他角色或第一章。基点 `d15e47b`，沿用 `codex/ch2-pacing-review`，仅本地、不部署。

## 生成与选择

- 本机 `auk-infer` / AuK-Flash 的 **speech-edit / whisper-conversion** 任务，不再仅通过同声朗读换随机种子。输入为原固定小凯声音派生的 v2/v3 同一句音频，保留原声音参考血统。
- 五个候选保存至 AuK `outputs/ch2-kai-whisper-v4-20260921/`；原始 WAV、成品 WAV/MP3、日志、源音频副本均保留。最终选候选 4，seed `2026092194`，实际指令为官方中文模板“用小声耳语的方式把这段话说出来。”。
- 新游戏文件：`app/public/audio/vox_ch2_natural_kai_whisper_v4.mp3`。使用新名规避旧音频缓存，原 v2/v3 不覆盖；保持既有播放器 0.45 增益。
- 后处理仅轻量起始静音裁切与响度目标 −23 LUFS / −3 dBTP，24 kHz 单声道、128 kbps MP3；没有变调、变速或剪断尾字。
- 候选 1/3 仍有较高的末词音高轮廓；2/5 虽较低但与旧 v2 波形高度相似，未当作明确重新表演成功。候选 4 有更明显的变化，末词主要有声部分约 130→122 Hz，作为低声私语方向的试听版本。
- 这些声学指标不等同人工听感。Qwen 对新候选都仍描述为有声说话，不能自动证明“悄悄话表演合格”；不宣称纯气声耳语。最终表现仍由作者试听确认。完整参数、实际指令、文件哈希、转写及自动感知结果见 `ch2-kai-whisper-v4.json`。

## 接入与验证

- `ch2-pacing-preview.html#kai-whisper` 主试听使用 v4，保留被否决的 v3 对照。预览不读写存档。历史作者轮 14 槽位试听/参数文件未重写。
- 新增 `app/tests/ch2-kai-whisper.mjs`：相对 `d15e47b` 仅一处声音别名变化、84 个旧音频哈希一致、共享代码不变；新 MP3 解码/哈希/ASR 完整“跟你说个事儿”、实际生成指令与处理链检查通过。
- 第一章冻结检查、精确历史投影、第二章剧情、同事支线及两轮旧声音审计通过。类型检查与生产构建通过；既有大包警告保留。
- 预览和媒体浏览器测试沿用桌面/手机场景，检查小凯入场实际调用 v4，原交互不变。
- 为保持历史断言可运行，仅把精确差异投影的该条 `after` 声音名更新为 v4，`before` 保持原 v2；追加新音频白名单，未覆盖或删除旧音频保护。

## 回退

本轮单独提交为 `fix(ch2): make Kai entrance a quiet aside`，可 `git revert` 该提交恢复 v3 接入。不要重置分支或清浏览器存档；原 v3 文件一直保留。素材试听地址：`http://127.0.0.1:8798/ch2-pacing-preview.html?v=kai-whisper-v4#kai-whisper`。
