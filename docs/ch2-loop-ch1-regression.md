# 第二章完整循环：第一章保护回归

实测日期：2026-09-24。发布基点 `05889fa`；本轮工作分支 `codex/ch2-loop-parity`。全部浏览器检查在 Playwright 启动的独立 headless Edge 和隔离存档中进行，没有读取或改写用户浏览器配置、标签页和真实存档。

## 通过结果

| 检查 | 结果 |
| --- | --- |
| `ch1-freeze-loop.mjs` | 30 个第一章/共用/DR/DSA App 函数逐段一致；剧情、完整 store、DR/DSA 数据、全局样式、入口、依赖与原有类型契约一致；263 个已发布图片/音频 Git blob 哈希一致，包含最近批准的老范、工人、气胸男三条配音。 |
| `ch1-night-loop.mjs`（原脚本未改） | 从无存档标题页真实开始，走完 254 个不同节点、五夜与五个自由探索菜单，到交班晨会。18 个一次性探索事件完成后隐藏；获得五枚夜班印章；没有 pageerror。 |
| 原 CR 读片 | 完整等待 6 段原有读片演出，没有注入跳过、改定时器或直接改进度。 |
| 原经营与探索 | 检查 4 次班后经营、13 次实际小卖部交互；免费逛店/翻书不扣行动力，咖啡实际恢复行动力，原保养流程继续可用。 |
| `ch1-shop-quiz-pacing.mjs`（原脚本未改） | 八种商品原价与原效果、耐用品原规则、第一夜钥匙门槛、每夜五张刮刮乐及刷新限购、购物勋章通过。桌面及手机横屏通过。 |
| 第一章考核 | 原题库五题真实作答、重复点选不重复计分、满分奖励只发一次、成绩页刷新不重复发奖，第二章成绩与 DR/DSA 存档保持原值。 |
| 第一章旧存档 | 缺失 DLC/背包/行动力/卡片字段的历史档按原默认逻辑恢复，不重置金币、旗标。 |
| `ch1-settlement.mjs`（原脚本未改） | 五个夜末节点在开发 StrictMode 中均只结算一次，耐久、金币、印章无二次变化，没有空白对话卡住。 |
| `ch2-pacing-runtime.mjs` | 五班结算、八种商品、独立计数、咖啡跨班、旧成绩重玩、考核持久化/防重复、第一章/其他 DLC 状态保护全部通过；送礼验收按获批改动移至当面对话。 |

走查截图与 JSON 日志位于工作仓库外的 `../ch1-loop-parity-review/`，不打包到线上游戏。目视检查第二夜前经营截图，确认原两列属性、设备保养、底部菜单、首次夜班勋章展示均保留。

## 历史基点差异没有隐藏

旧 `ch1-freeze-pacing.mjs` 原样保留。它固定对比 `1452d78`，本次执行在 `store.ts` 的三条配音文件名映射处失败；差异来自此前用户已确认并上线的声音补丁，并非本轮代码变更。不能把它写成“通过”。

本轮新增 `ch1-freeze-loop.mjs` 固定对比用户指定的已发布 `05889fa`，不自动刷新基线，严格覆盖原有逻辑和全部已发布媒体。新字段只允许 `dlc.ch2` 的五个可选成员，新增接口限定为第二章账本三种类型。

`ch2-pacing-runtime.mjs` 的历史基点仍为 `1452d78`：只精确反向投影已批准的三条配音映射（完整字面量必须匹配），没有允许任意 store 改动；新增 App 导入也使用明确的第二章模块白名单。原共享代码比较和所有奖励数值断言均保留。

原先“班后分享”断言按用户批准方案改为：班后调用无副作用，物品保留；在小唐/老周对话中实际消费，奶茶不二次加人心、零食人心 +1、重复递送不重复奖励。并未删除该项覆盖。

## 复跑命令

在 `app/` 下设置 `PLAYWRIGHT_MODULE` 为本机 Playwright 安装路径、`CHROME_PATH` 为 Edge 可执行路径，依次运行：

```text
node tests/ch1-freeze-loop.mjs
node --import tsx tests/ch1-night-loop.mjs
node --import tsx tests/ch1-shop-quiz-pacing.mjs
node --import tsx tests/ch1-settlement.mjs
node --import tsx tests/ch2-pacing-runtime.mjs
```

以上是第一章保护子集；第二章观察、扫描、完整剧情、新经营与视觉回归由本轮主交接记录汇总。这里不声称修复此前暂停处理的 Edge 偶发卡死问题。
