# 第二章：作者指定配音修订（2026-09-21）

分支 `codex/ch2-voice-author-revisions`，基点 `7032d58`。仅本地，不推送/部署。

## 最新裁定（优先于上一轮8字限制和14条全重配）

| 角色 | 本轮结果 |
| --- | --- |
| 老周 | 你来扫，我去泡杯茶。 |
| 小凯 | 跟你说个事儿。 |
| 小雷 | 新机器接入packs了。 |
| 小何 | 老爷子夜里坠床，要拍头颅CT。 |
| 主任白班 | 年轻人，动作快起来！ |
| 主任晨会 | 恢复原录音：年轻人不错啊，我出几道题考考你。 |
| 雯雯 | 恢复原录音：加班加到十点的销售，您见过没？ |
| 值班医师 | 不配音；原有文字、电话头像及流程保留。 |
| 复查大爷 | 不配音；文字、立绘、结果和奖励保留。 |

「进机器」已向作者确认，明确应为「新机器」。音频输入用英文单词packs，避免P-A-C-S逐字母；剧情字幕保留标准缩写PACS。小何送入模型的CT写成C T，原句不删词、不硬凑8字。

小唐、老范A/B、孩子父母等未被指定修改的上轮版本保持不变。神秘病人、陆舟男女、问糖、腹痛、催号和原低音量哭声仍保持；不恢复其他已停用患者声音。

## 具体改动

- `app/src/game/ch2.ts`：5处新配音及对应开场正文，2处恢复到 `1d7342d` 的原音轨/对白，2处仅移除sfx。共9个已有节点；完整前后字段见 `ch2-voice-author-changes.json`。
- 五个新音频为 `vox_ch2_natural_{zhou,kai,lei,he,director}_v2.mp3`；原始WAV、参考副本、种子和日志在仓库外 `../AuK/outputs/ch2-voice-author-20260921/`。不覆盖上一轮或任何共用MP3。
- 原晨会调用 `vox2_director_am`，原雯雯调用 `vox2_wen`；原文件字节保持不变。静音场景不以静音MP3占位，不误播之前的旧轨。
- 试听页原地址保留，显示「本次重配/恢复原配音/保留/不配音」。14个列示场景里12条可播，静音两位没有播放器，也不会进连听队列。此页不读写存档。
- `ch2-voice-current.json` 为试听页当前14槽位清单；五条作者指定台词、两处恢复与两处静音在 `ch2-voice-author-lines.json`。
- 生成/检查脚本增加独立参数记录路径和显式作者句长覆盖；旧脚本默认仍使用旧计划，新轮用 `scripts/ch2-voice-author-generate.py` 和 `scripts/ch2-voice-author-qa.py`。
- 仅忽略脚本生成的 `scripts/__pycache__/` 本地字节码；缓存保留在本机，不随源码提交。
- **App、store、types、患者/探索模块、第一章、DR/DSA、总线、图片、交互、选项、奖励、状态结构均未变。** 不清浏览器进度，不改变完成入口隐藏，不加入新声音触发点。

## 生成与质量记录

AuK-Flash + Qwen2.5-Omni-3B，bf16、cuda:0、cpu_offload；全部使用同角色固定参考、官方精确零样本模板。保留上轮音色参考哈希，不在指令末尾追加容易被朗读的表演说明。五条均为take/attempt 2。24kHz单声道128kbps MP3，约−20 LUFS、−2 dBTP处理，成品约1.71—2.77秒，无变速变调。

完整生成命令、种子、时长、参考/输出SHA-256见 `ch2-voice-author-generation.json`；声学/ASR见 `ch2-voice-author-qa.json`。

核对限制照实记录：
- 老周、小凯、主任的Whisper转写与指定台词一致。
- 小雷被Whisper和独立音频模型转写为「Pax/pax」（与packs同音的英文拼写），没有转写成逐字母P-A-C-S；不能据此冒称人工确认了精确口音。
- 小何快句的Whisper输出为「老爺在夜裡墜床 要拍投錄CT」，存在错字/分词；另一个本地Qwen音频模型独立转写为完整「老爷子夜里坠床要拍头颅CT」。原始分歧保留，没有伪造逐字ASR全通过。模型对CT/连读的泛泛判断不作发音认证。
- 本轮没有人工回听认证；最终听感、儿化与packs发音请作者试听。自动记录在 `ch2-voice-author-listening-model.json`，不当成主观批准。

## 验证与回退

- 本轮静态测试以 `7032d58` 为基点：只有9处列明的text/sfx差异；完整图结构、选项、元数据、状态差分及范围外源码一致。全部旧音频未覆盖。
- 前两轮测试增加显式、先断言再还原的作者修改投影，继续保护58处叙事和旧14条音频包；不是删除旧断言。当前用户裁定另由本轮测试完整验证。
- 隔离Edge逐场确认当前12条实际播放并结束、2处没有人声调用；原heard标记不阻挡；试听连听/停止、手机布局及不写存档检查通过。
- 构建/类型、ch2-data、ch2-continuity、本轮及既有叙事/声音静态检查通过。构建有原有大包警告；lint维持原基线10 errors/2 warnings。未重跑全部剧情结局，也未声称根治真实Edge随机卡死。

试听：`http://127.0.0.1:8798/ch2-voice-preview.html`；游戏：`http://127.0.0.1:8798/#/ch2`。刷新即可，不需要重置存档。

用 `git revert <本轮提交>` 撤销本轮；对照基点 `7032d58`。不要hard reset，也不要按旧14条重配清单恢复本轮静音/原版裁定。
