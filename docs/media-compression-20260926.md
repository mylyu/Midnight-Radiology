# 全游戏交付压缩至15 MB以内

基点 `ac703eb56d74584d1076ca71efc99fd3cd1d242f`，分支 `codex/media-under-15mb`。作者明确授权自行压缩、发布GitHub/Pages、导出Kimi包。之前提供的Kimi ZIP运行媒体与基点相同，未采用其中代码或覆盖现版。

## 实测体积

完整生产目录316文件：**29,528,353 → 14,933,783字节（14.934 MB，减49.4%）**，按十进制15,000,000字节验收，不以ZIP/gzip大小代替。

| 部分 | 压缩后字节 |
| --- | ---: |
| 全部WebP（含12份切片atlas） | 9,077,588 |
| 三张调窗数值PNG，原样保留 | 2,689,388 |
| 100条MP3 | 2,137,815 |
| JS/CSS/HTML及其他 | 1,028,992 |

章节加载（含共用大厅资源）：第一章88文件/约2.92 MiB，第二章145文件/约6.65 MiB，DR及DSA各24文件/约0.92 MiB。各章共享文件有重叠，不能相加当部署大小。源码交接ZIP还含文档与测试，因此与部署目录不是同一统计口径。

## 图片：177改动，23原件保护

- 200个逻辑ID不变，177张静态图机械缩放/编码；无重绘、无裁剪、无内容编辑。背景通常最长边≤1280，普通医学插图≤1024，立绘≤768，小道具≤640；不放大。WebP effort6、alphaQuality100，具体尺寸、质量值和新旧SHA256见同名JSON。
- 第一轮按用途尝试q68/62/56，医学图q72/66；第二轮需要进一步缩小的立绘/道具使用**原基点文件**编码q68，不从首轮候选再次压缩。只接纳更小文件。不强行让所有图≤80KB，避免毁掉文字和透明边缘。
- 23份保护原件包括三个调窗数值PNG、CT运动床/机架图、文字证物、修正钥匙和通关印章；全部哈希不变。12份CT序列atlas亦不变，位置/运动与病灶观察配置不改。
- 查看全部177张的15页前后对照；对手腕裂纹、主动脉夹层、针状金属影、母子像素立绘另看较大对照。未见新裁切、明显透明边缘损坏或关键特征丢失。有损图放大仍会更柔和，不能宣称像素级无损。
- 导入使用新增 `--keep-webp`，原样接纳已审核字节，保留原有尺寸/alpha/哈希检查。177份旧交付文件在确认无catalog引用后移出public，保留外部备份和基点Git历史。没有删除历史文档、原始设计档案或游戏内容。

## 音频：保留表演，仅转码

原100条共3,493,840字节；67条64 kbps、3条96 kbps、30条原件，总计2,137,815字节。保留原采样率、声道和解码样本数，不做变调、加速、增益或剪切，不重新生成角色声音。

候选失败则回退96 kbps，再失败保留原件。接受阈值：波形相关≥0.97、信噪比≥15 dB、RMS变化≤1 dB、新增削波样本≤3。实际修改音频最低相关0.9861、最低SNR15.40 dB、最大RMS变化0.514 dB。所有文件可解码、无截尾/样本数改变。

**限制：这些是客观解码与波形检查，不是逐条人工回听认证；不能据此宣称听感完全无差异。** 当前工具未提供可靠的直接听觉审核，本轮明确告知作者后采用较保守64/96 kbps，不使用建议中的全量32 kbps/22 kHz单声道。

## 验证与工程记录

- `npm run build`：类型检查、资源生成、生产构建、完整dist体积检查通过。Vite既有大JS chunk提醒保留，没有通过放宽阈值消音。
- `node --import tsx tests/chapter-asset-manifest.mjs`：全部剧情/条件/旧档/手册/音频/atlas资源、哈希/字节/章节隔离通过。
- `CHAPTER_LOADING_FILTER=^cold-(ch1|ch2|dr|dsa)-full-offline$`，运行原 `chapter-loading-browser.mjs`：真实Edge四章加载100%，下载并发≤4；断网后分别解码62/117/16/15张图和26/28/8/9条音频，进入各章不补请求，页面错误0。第二章390px，其余桌面截图已看。音频play拒播模拟与真实音频解码明确区分，不冒称试听。
- 七个修改/新增脚本定向ESLint通过。未改App及剧情/交互源码，故不重复全章通关或递归历史冻结；没有改宽旧测试、重写历史认证账本。原App遗留lint不在本批次处理。
- Windows导入期间遇到catalog短暂EPERM文件锁：保留已完成导入，按精确新旧路径/哈希恢复；仅对该rename错误最多重试两次，不忽略其他失败。最终所有素材导入、清单和构建通过。
- 本机候选、原件、对照页及浏览器截图：`C:/Users/lvmen/AppData/Local/Temp/midnight-media-small-Sk99Pc/`。它是临时工作证据位置，不是公开下载链接或唯一备份；可回退素材在Git基点。审计JSON只保留相对交付路径与哈希，不将本机绝对路径当许可来源。

## 继续开发 / 回退

`npm run build`新增postbuild全目录15 MB预算检查，GitHub Actions同样执行。余量约66 KB，后续添加内容先优化或申请调整预算，不破坏调窗数据、不关闭检查。既有章节完整加载和内容哈希更新继续有效；同名MP3内容变化已写入生成的章节manifest。

本轮为一笔媒体/工具/交接提交（见 `git log ac703eb..codex/media-under-15mb`），可 `git revert <该提交>` 同时撤回新图、音频、catalog和预算工具，然后重新构建发布。不清存档、不hard reset、不强推。旧文件还在Git历史，不能只回退catalog而漏回文件。保留冠脉ImageCAS非商业许可及商用前替换要求，压缩不改变素材来源或权利。
