# 第二章滑动打卡：保护与回归记录

本轮固定基点为 `b3213f0`。只在第二章五个实际工作班次增加滑动打卡；晨会不是额外工作班，不增加打卡。第一章每日打卡及日期、连续天数、奖励全部沿用原实现。

## 五个入口与奖励

| 现有节点 | 班次 | 完整滑动后 |
| --- | --- | --- |
| `c2n1_1` | 第1夜 | 原有金币 +50、行动力 +3 |
| `c2d2_0` | 第2日 | 登记到岗，无新属性或经济奖励 |
| `c2n3_1` | 第3夜 | 原有金币 +50、行动力 +3 |
| `c2d4_0` | 第4日 | 登记到岗，无新属性或经济奖励 |
| `c2n5_1` | 第5夜 | 原有金币 +50、行动力 +3 |

滑动门禁在第二章演出阻塞逻辑中工作。尚未完成时，不执行原节点效果、打卡音效或自动添加已应用节点；点击背景可以补全文字，但不能推进过门禁。完成时使用原 `ch2.ts` 节点效果，在同一次存档更新中记录效果、`c2_checkin_*` 旗标、原 `appliedSteps` 键和原流水ID。已有流水去重与父组件内的同步引用防止重复回调、随后节点效果和刷新重复发奖。

原有三次夜班打卡总计仍为 +150 金币、+9 行动力；没有新建奖励表或共享存档字段。两次白班只增加无经济变化的登记流水。完成后留在原剧情节点，继续播放原文字。

旧存档若已有原节点应用键、已有对应流水，或属于只有当前节点而没有 `appliedSteps` 的历史格式，不要求补做已经自动完成的打卡、不重复发奖。新待打卡存档含明确的 `appliedSteps` 数组，刷新仍需完成打卡。显式重玩沿用原第二章重置逻辑，清除 `c2_` 旗标和第二章进度。

## LIVE冻结与历史逆投影

- `ch2-checkin-freeze.mjs` 对固定 `b3213f0` 核对103个既有源码、配置、剧情总线与历史清单，禁止本轮修改原第二章故事、奖励、购买、结算、扫描、共享存档、第一章/DR/DSA、依赖或全局样式。
- `App.tsx` 的31个非 `Ch2Screen` 函数、原导入、全部顶层状态与路由定义逐段一致。只允许两条准确的新导入及 `Ch2Screen` 内接线，没有新增共享函数。
- `ch2-checkin-source-deltas.json` 固定记录本轮 `App.tsx` 的9处准确hunk。逆投影检查真实当前内容后，必须逐字恢复成 `b3213f0` 原文件；11个区内/区外破坏探针全部必须被拒绝，不能只用可变清单接受任意改动。
- 新源码路径只允许 `Ch2Checkin.tsx`、`Ch2Checkin.css`、`ch2-checkin.ts` 三个文件；不放开整个第二章目录。
- 288份原图片/音频逐份核对Git blob；没有新增、覆盖或删除图片和音频。打卡设备由隔离组件与限定作用域的CSS绘制。
- 历次 `source-deltas.json` 清单及历史基点保持原样。新LIVE保护还明确核对旧清单，不把旧保护改成最新快照。

旧测试只增加以下适配：

1. `ch2-payoffs-freeze.mjs` 首先执行本轮LIVE保护，然后才从历史新增源码计数里分离三个已经验证准确路径的新文件；原 `6a0311b` 基点、100旧文件、282原媒体、6个独立固定哈希新图及其他断言不变。
2. `ch2-payoffs-projection.mjs` 先精确剥离本轮App接线，再执行原有回礼/进床逆投影。旧日出、悬疑、循环、对白等历史测试通过既有调用链自然衔接；原历史清单与逆投影断言不删减。
3. `ch2-checkin-driver.mjs` 为浏览器测试执行真实按下、横向拖动、松开滑块。`ch2-repeat-walk.mjs`、`ch1-ch2-continuous.mjs`、`ch2-loop-browser.mjs`、`ch2-flow.mjs` 只在第二章推进循环遇到门禁时调用该动作，保留原断言，没有注入完成旗标、跳转节点或缩短剧情计时。

## 非浏览器验证

新 `ch2-checkin-data.mjs` 覆盖五次原子完成、原三夜总奖励、白班无新奖励、重复回调、JSON保存与恢复、旧存档、结束存档、过期/非剧情回调、显式重玩以及第一章/其他DLC状态隔离。

在 `app/` 下用 `node --import tsx tests/<文件名>` 运行上一轮 `ch2-payoffs-regression.md` 列出的全部42项非浏览器回归，另加本轮 `ch2-checkin-data.mjs`、`ch2-checkin-projection.mjs`、`ch2-checkin-freeze.mjs`。本轮分三组各15项执行，三组均为 `TOTAL 15 FAILED 0`，合计45项全部通过。旧 `ch1-freeze-loop`、`ch1-freeze-pacing`、日出/悬疑/回礼冻结以及全部历史逆投影探针均通过，历史基点没有刷新。

该保护记录不把静态断言视为浏览器操作，也不声称修复先前另行暂停处理的Edge偶发整页卡死问题。

## 浏览器与界面验证

`ch2-checkin-browser.mjs` 在独立、无用户资料的 Edge 测试上下文运行。开发端 `8798` 与生产预览端 `8801` 各通过28组场景，均无页面异常：五班各一次鼠标与真实CDP触摸、两组横屏键盘操作、15组旧存档格式、通关存档恢复。用例检查进入原前驱节点后自然到达打卡、未滑完松开回弹、到终点未松手不发奖、取消指针、真实切换标签页、滑动中刷新、完成后刷新、原文字继续及跨章数据隔离。定向旧档/移动端使用明确的存档与音频拒播夹具，不冒称从头通关。

- 桌面1280×900、手机390×844、横屏844×390及640×360已截图目视检查；滑块可见可触达，无水平溢出。键盘先移至终点，再按回车确认，直接回车不能绕过。
- 测试驱动先关闭原游戏的“建议横屏”提示，再操作滑块；短滑后等待原240毫秒回弹完成。测试真实失焦时关闭Playwright默认焦点模拟。这些是测试环境适配，没有移除游戏提示或修改交互计时。
- 独立试玩页 `ch2-checkin-preview.html` 复用正式组件，可反复试滑，不读取/写入存档、不发奖励；手机上的成功/重试与存储保持原样已经检查。该页是Vite本地入口，不在默认生产构建中发布。
- 结果及截图位于仓库旁 `ch2-checkin-review/` 和 `ch2-checkin-review/production/`，不将测试图片打入游戏资源。

复跑命令（在 `app/`，先确保相应开发/生产服务运行）：

```powershell
node --import tsx tests/ch2-checkin-data.mjs
node --import tsx tests/ch2-checkin-browser.mjs
$env:GAME_URL='http://127.0.0.1:8801/'
$env:CHECKIN_OUTPUT='../../ch2-checkin-review/production'
node --import tsx tests/ch2-checkin-browser.mjs
Remove-Item Env:GAME_URL, Env:CHECKIN_OUTPUT
```

`npm run build`（含TypeScript项目构建）通过。本轮组件、逻辑、浏览器驱动及适配的四个走查文件定向ESLint通过；全仓lint仍为原有 **10 errors / 2 warnings**，无新增。原有超过500kB的包体警告保留，没有提高阈值或关闭检查。

## 第一章到第二章连续走查

2026-09-24，`ch1-ch2-continuous.mjs` 的 `female-curious` 路线从空标题页开始，只固定随机种子，不注入存档。第一章五夜262剧情节点，完成原探索、CR读片与晨会后，通过大厅进入第二章；第二章411剧情节点、481推进轮，五个打卡节点全部真实拖动确认，17次采集/重建、12次观察、五班结算、五道晨会题和尾声全部完成。

原账本逐笔对账、事件ID唯一性、考核仅一次领奖、结算刷新、第一章继承状态对比及通过大厅真正启动第二章重玩均通过。0未捕获页面异常、0失败网络响应。第二章完成后刷新不重奖、不倒退；重玩清除第二章本轮成绩/线索，保留第一章与共享财产。该结果是一条完整自动浏览器路线，不宣称人工逐句通关或穷举所有分支。

```powershell
$env:CHAIN_VARIANT='female-curious'
$env:CHAIN_OUTPUT='../../ch2-checkin-continuous'
node --import tsx tests/ch1-ch2-continuous.mjs
Remove-Item Env:CHAIN_VARIANT, Env:CHAIN_OUTPUT
```

结果、逐节点操作记录和结尾截图在仓库旁 `ch2-checkin-continuous/`。实现提交 `d7868ea`，回归交接提交可用 `git log --oneline b3213f0..codex/ch2-slide-checkin` 查询；逆序revert交接提交再revert实现提交即可撤销本轮，保留上一轮钥匙/回礼/进床成果。未推送GitHub或部署Pages。
