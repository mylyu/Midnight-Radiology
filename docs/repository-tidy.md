# 仓库首页整理 · 2026-09-25

- 用户要求清理GitHub仓库首屏杂乱的中间文件，并突出在线游玩链接。
- 基点`485bc41`；分支`codex/repository-tidy`。生产源码、公开素材、构建配置、Pages工作流、剧情总线和原有测试审核记录不搬动、不改内容。
- 81个文件只迁移：79个历史试听/旧配音/图片备份/脚本/日志移至`archive/original-development/`；2份配音台词移至`docs/voices/`。每个文件的旧路径、新路径和基点Git blob均记于[清单](repository-tidy-moves.json)，可核对内容未变。
- 根目录仅保留当前项目入口、应用/设计/文档/工具/归档目录以及许可证；README顶部直接显示GitHub Pages游玩入口，增加清晰目录与开发导航。
- Kimi导出工具更新两份底稿路径。旧`pw_*.js`是早期环境的一次性脚本，已归档并从当前交接包选择中去掉；当前`app/tests/`完整保留。没有删除历史文件，没有改动原始候选，也不重写Git历史。
- 现有`docs/`审核记录、`深夜影像科/`路径与`HANDOFF.md`保持不变，避免交替开发和精确冻结测试失效。新的本地根目录日志/缓存加入忽略规则；历史日志依旧受版本控制。

## 验证与回退

- 新测试`app/tests/repository-tidy.mjs`核对全部81份旧/新文件Git内容标识、旧位置消失、生产目录与基点完全一致，以及README入口。
- 发布前执行生产构建、原文档/章节保护及Kimi导出`-PlanOnly`；该检查不生成额外ZIP。
- 已通过：81份内容/路径验证、README及导航本地链接、类型与生产构建、总线精确保护、第一章/DR/DSA 30个共享函数与263份旧媒体保护。构建仍为`index-CCAaJmXY.js` / `index-gs3iXliY.css`，保留原大包警告。
- Kimi导出预检通过：763个文件，两份新位置底稿必需且已选入，归档候选/旧脚本全部排除；未生成新ZIP。现有测试与冻结基点没有放宽或迁移。
- `git log 485bc41..codex/repository-tidy`查本轮提交，`git revert`即可还原整理；不要hard reset或清理玩家存档。归档是目录整理，不减少已有Git历史体积，也不改变网页加载内容。
