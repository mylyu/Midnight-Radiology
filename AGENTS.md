# 《深夜影像科》协作开发约定

适用于本仓库全部开发者与 AI（含 Kimi、Codex、DeepSeek）。根据第一、二章反复开发和作者裁定整理。**交付目标是延续已经好玩的游戏，不是展示新架构、堆功能或堆测试数量。** 作者本次明确要求优先；历史方案不是永久待办。

## 1. 开工：先认版本，再选最小改动

- 先看 `git status`、当前分支/提交和 `HANDOFF.md` 顶部最新记录；不要覆盖另一位开发者的未提交工作。区分本地、已提交、已推送、已部署，不能互相冒充。
- 本文件是日常规则入口。剧情任务再读 `深夜影像科/全书剧情总线.md` 的相关章节、人物与伏笔，以及相邻实际对白；技术任务只读有关实现和对应记录。**不要每轮重读全部交接、全部历史对话、全部审计账本。** 新章策划需通读总线并核对前章承接。
- 判断现状以实际代码/素材和作者最新批准为依据；总线约束叙事。遇到冲突先区分“已实现事实 / 未来规划 / 已否决旧稿”，有实质分歧先报告，不自行选一份旧稿覆盖现版。
- 动手前简述：本轮改什么、不改什么、如何做最小验收。明确任务直接做；只有会改变方向的缺失决定才询问，不重复索要已给出的批准。
- 在 `codex/<主题>` 分支做可回退的小提交；其他协作者遵循所在环境的分支规范。不得自动合并被否决的 `codex/ch2-rework` 或归档原型。
- 修第二章不顺手重写第一章、DR、DSA。确需改公共逻辑时先列消费者与影响；默认保留剧情、选项、奖励、配音、存档 ID 和主交互。

## 2. 定位与写作：BME 的游戏，不是临床操作考试

- 面向 BME 工科学生：设备构成、成像物理、重建/显示、参数与取舍。病例是故事和工程问题的载体，不把课程写成护士流程、技师检查单或处方训练。
- 先有患者、动机、冲突和连贯对话，再自然带出知识。不给每个角色塞一段定义或人生总结；避免格言腔、爹味、机械口令和“为了学生理解”的长篇说教。
- 日常对白短、具体，有反应、有接话对象；可以吐槽、犹豫、开玩笑、问同事、去翻书。不靠每个人说一句莫名其妙的话拼剧情，不把探索改成卡片列表。
- 患者之间留合理交接/闲聊，日出、日落等过场先有行动动机与时间过渡。不是每段都要恐怖或抒情；同事关系不要默认暧昧，也不要让小唐包办所有关怀。
- 悬疑先铺垫，隔班有回响，按总线闭合或留白。不同暗线不硬并；不给设备无依据的读心/联网能力，不因制造悬念拖延必要诊疗。
- **先让玩家看、选、求助，再解释。** 提问应改变观察方式或产生自然回应，不是点击六格走流程。不提前用字幕、弹卡、红圈、选项颜色泄底；同一病例不叠重复题。
- 知识深度服从课件与作者认可。可选手册容纳补充知识；不擅自引入复杂指标/计算题。DR 已允许 DQE、SR、非晶硅/硒、CsI、GOS，不据此扩展到一整套高级信号评价课程。
- 科学事实不牺牲：图文部位/年龄/方向/时相对应；屏幕左右不当患者左右；单张示意不代表完整诊断。主角负责设备与图像，医师确认诊断/处置。新增医学硬事实核对可靠原始资料，来源记文档，不把游戏写成指南。
- 现有晨会随机五题、姓名学号和通关凭证属于普通章闭环；“不生硬插题”不等于删除已获批考核。DLC 保留自身形态，不强套五夜与发证。

## 3. 新章节必须继承的体验

开新普通章时先列下面这张对照表：**沿用哪个现有实现 / 仅替换什么内容 / 有意差异及理由**。先做一班的完整闭环，再批量填五班；不要只写病例，最后才补商店、数值、回礼和结算。

| 必须考虑的环节 | 约定 |
|---|---|
| 进入与续玩 | 本章完整加载、开始/继续、自动存档；打卡沿用对应章已确认交互 |
| 自由探索 | 合理医院地点、行动力、可选同事/物品支线；完成的一次性事件隐藏；独立未完成事项仍可返回（如第二把钥匙） |
| 病例 | 患者出场→问题/方案选择→采集演出→图像/观察→人物回应，不无缝接下一位患者 |
| 同事与背包 | 有物品且对方在场才送礼；闲聊中消费并回应，不在班后统一送礼、不打断抢救 |
| 班后经营 | 有场景的完整页面、金币/三属性及真实本班变化、设备状态、背包、章节入口、商店/旧书/手册/勋章；主动点下一班才推进 |
| 收集与回响 | 商品有明确用途或可追踪隐藏影响；书页分班解锁；行为完成才颁对应勋章；此前得到的物品、帮过的人后来有条件回响 |
| 章末 | 最后一班经营→晨会→真正尾声/回礼/后续预告→确认姓名学号及本章凭证；不提前打“本章完” |

- 继承第一章的布局、操作和循环，**不是照搬 CR 耐久/维修费到 CT，也不是把三夜两白改成连续五夜**。章节名和解锁状态从当前事实核对，不另写冲突名单。
- 先找已有组件/函数/数据配置；能复用就复用。适度抽取已有的重复能力，不为做一章先整体重构第一章引擎或发明大框架。
- 已下架/否决的设计不能“按旧计划补全”：CT 六格检查单、陆舟本章来访/水模环状伪影、满屏AI素材提示、额外“显示全文/继续”按钮等。陆舟**章末已知联系人来电**是后来获批的例外，不误删。
- 关键前情：旧进口64排 CT 与走廊蒙布老 CR 是两台设备；老周返聘不等于已经离岗；1998、神秘病人、盒子各自按总线推进。旧选择/礼物只有真旗标才回响，不给新档硬塞共同记忆。

## 4. 交互、存档与数值的硬约束

- 保留点文字补全文、小三角提示、原选项操作，不擅自改对话界面。
- **两章选项：全文后900ms缓冲 + 距上次点击至少300ms。所有点击都计时，包括被拒绝的点击。** 持续连点不能选中刚出现的答案，停手后新手势可选；不反复隐藏选项。
- 按下时未就绪、出现选项才抬起的手势，以及键盘长按 repeat，不得选中。送礼、观察、求助返回即便节点 ID 不变，也视作新的对话呈现。普通翻页300ms，购物/保养500ms同步防重。不要恢复“只记录有效点击”的旧优化。
- 音频结束不是剧情解锁条件。拒播、资源异常、切后台不能产生永久点击锁；跨场景取消迟到回调和待播语音，不回放已离场人物。
- 选择结果、随机结果、实际属性差值、目标游标原子保存；节点自身奖励另按既有机制去重。不要在 React 可重放 updater 中执行随机/保存副作用。动画、打卡、考核、消费刷新不重复结算。
- 保留第一章7处固定30%“好心办坏事”，刷新不重抽，属性不改其概率。其他章按各自已批准概率，不能把所有善意选择都套30%。
- 医术、人心、家业累计，不因UI只有五个图标而封顶/扣回；显示准确数值和真实差值的**无声冒泡**。低属性不堵住安全路线，高属性不自动替玩家诊断。
- 满耐久不扣维护费，部分恢复显示实际恢复；重复购买无收益的持有物不再扣钱，可消耗物/即时生效商品按原规则处理。班末工资不在结算页重复发。
- 跨章继承、身份、旧凭证、其他DLC进度保持；重玩只清本章本轮状态，包括旧考核成绩。旧档缺信息就兼容/注明，不能补造奖励、回忆或强迫清存档。
- 勋章/卡片分母对应现役可获得项目；停颁旧收藏保留。评估正常路线可达性与互斥，不为凑满收集擅改分支。
- **到过地点≠完成里面所有事项。** 有多个可独立发现的物件时，检查“先A后B、先B后A、提前离开再回来”；已付进入成本不重复扣，完成内容仍隐藏。入口因行动力不足消失时给出补充行动力的提示，不让玩家误以为删了剧情。

## 5. 画面、声音、加载

- 人物看**第一章实际立绘**：粗颗粒像素、深色轮廓、统一比例/色调/透明边缘；不要只在提示词写“pixel”就当风格通过。母子、同一角色不同场景必须看着是同一个人。
- 正常患者能走就走、需要时用轮椅，严重情况才平车；不要把所有人画成卧床。白班用白天背景；场景应有真实空间/设备关系，不设“球管间”“平板间”来凑知识地点。
- **当前节点决定当前立绘**，不能无条件继承上一人物。公共菜单/短信/无人场景显式退场；远程消息不等于实体在场。切场和旧档恢复第一帧就正确；真实当面交谈、礼物回应不得一刀切清空。
- 图像匹配病人、部位、年龄、平扫/增强、新旧日期；先看图再设观察答案。薄厚层应看得出差异；细微骨折不画成裂谷。找不到合适素材说明缺口，不拿成人替儿童、头颅替手腕。
- CT 中区分：投影重建断层、同次断层做三维渲染、实际补扫。现有采集3秒不可跳过，床/切片/声音共用时钟；三处连续补扫用控制台，不假称“仅重建”；工作站处理不重新进床。不要为让测试更快改真实演出时长。
- **不加普通对话/走路嘟声。** 角色短出场音自然生活化，一般不超过8字，作者指定句子例外；不是人人都需呻吟/配音。批准的音色、音轨、音量不顺手重做。
- 配音先处理表演、重音、停顿和人物年龄，不以升调/加速冒充演技。多句保持角色一致；是否用固定参考服从最新要求。小凯已批准无 reference 候选B，不恢复早期重“事儿”、上扬版本；PACS按已批准 packs 连读，不拼字母。
- 主观问题先做小样、看实际画面/听实际片段再批量；ASR正确、时长合规不等于好听，图片加载成功不等于画对。没有直接听验/看验能力就明说，不声称完成感官验收；通常一至两轮仍无改善应定位原因/提供有区别方案，不盲目连抽。
- 展示图优先有损 WebP（透明图也可有损），精细文字可无损WebP；调窗数值源不做有损压缩。用 `docs/media-import.md` 的导入器、稳定ID/catalog/内容哈希；默认单图≤600KiB。扫描切片短暂显示，优先小图集，不塞大GIF或整套医学原始数据。
- 原图、试听候选、日志不放正式 `public/`，不在仓库根堆中间文件。删过期资源先查全部引用/动态清单/可恢复副本；只删明确目标，不借优化清掉设计资料。素材来源、许可、生成参数/模拟方法写开发记录，不反复塞游戏AI警告；ImageCAS商用前须替换或获授权。
- **分章节完整预载，不恢复边玩边临时联网的旧模式**：大厅小资源＋本章全部分支/旧收藏，真实字节进度，最多4并发，完整校验才准进入。压缩Blob供所有消费者使用，不整章解码常驻；哈希缓存只更新变化文件，失败可重试/返回、不假100%。加载阶段不提前改存档/领奖，新增动态资源必须登记。

## 6. 验证：以风险选测试，不以遍数证明负责

详见按需阅读的 `docs/validation-guide.md`。**本节优先于旧交接中“每轮两遍完整通关/全历史冻结”的默认做法；作者本轮明确要求的专项验收除外。**

| 改动风险 | 默认最小验证 | 不默认做 |
|---|---|---|
| R0 文档/交接 | diff、链接/路径、相互矛盾；打包规则改动只跑PlanOnly | 构建、浏览器、游戏通关 |
| R1 台词/立绘映射/局部CSS | 问题复现＋改动点及前后场景；1个负例，相关数据/定点UI检查 | 完整五班、第一章→第二章、全历史哈希链 |
| R2 商店/奖励/探索/单章状态 | 相关单元/数据测试＋正常/另一分支＋刷新去重/旧档；一次构建 | 所有角色/所有性别/所有路线笛卡尔积 |
| R3 共用输入/存档/加载、主循环/新章集成 | 相关边界故障测试＋一次相关章完整主线；确有跨章影响时一次1→2 | 无理由再跑第二、第三遍；无关DLC全通关 |

- 验证主动耗时目标通常占本轮10–25%，不是90%；小补丁浏览器验证目标5–10分钟内，中等修改10–20分钟，跨章高风险单独说明预算。这是**止损提醒，不是超时就算通过**；失败必须修复或如实列阻塞。
- 开跑前列“改动→风险→最小可证伪测试”，通过就停。想再跑一遍必须指出尚未覆盖的具体风险；“更放心”不是理由。
- 场景依赖前章选择时，直接切换相应旗标核对两条分支的台词/语音/背景，不以一张正确截图代表另一分支也正确；先确认触发条件，再判断是否素材错配或近期回归。
- 优先复用现有测试、指定节点夹具、固定种子、函数级边界断言。夹具只放隔离测试存档；定点回归不能冒称完整通关，流程脚本不能替代画面/台词审美。
- 只对改变语义的测试、对应消费者和必要冒烟重跑。同一构建、输入、脚本已有通过结果就复用；不让多个agent重复跑同一流程，不每轮重建整套审计体系。
- 保留历史测试与审核证据，但**普通小修不新增一层全仓冻结/源码反演/projection账本**，不递归跑全部历史冻结。用当前基线diff和针对性断言证明范围；确需改旧预期时解释作者已批准的精确差异，禁止删测试、放宽断言、重写旧哈希来掩盖回归。
- 视觉任务选实际受影响截图（通常桌面＋390px），语音任务听改动片段和必要同角色对照；不反复逐条全游戏验音。发布只核对构建版本、部署结果和受影响线上入口，不在线再全通关。
- 自动化优先等待真实状态而非长sleep；保留必要300/900ms输入等待及3秒采集规则。一次生产构建供本轮测试共用，测试中不覆盖dist；缓存测试之外避免反复冷下载。
- 现有lint遗留与新增分开，不拿历史问题阻塞无关小补丁，也不声称全绿。若验证超过预算，先排查重复跑/死等/测试基础设施；说明原因再决定缩小或升级，不默默消耗数小时。

## 7. 交接与发布

- 修改后在 `HANDOFF.md` 顶部用短条目写：请求/基点分支、改动及未动项、验证范围/结果、已知限制、回退、是否发布。需要细节再链接专题文档；不要复制整份历史或用几万次断言数字充当质量结论。
- 提交按实际可独立回退的工作划分，不为一行改字强拆三层。记录 `git revert <本轮提交>`；Git不能恢复浏览器存档，不用hard reset或清用户存档代替修复。
- 默认本地完成。**本轮明确授权**才推送/部署；发布到main会触发Pages。上线后检查真实站点的构建与资源路径；别只看localhost就说已上线。
- Kimi导入包必须带本文件、HANDOFF、总线、源码和正式素材，不含node_modules/dist/.git/原始大素材/试听废稿。用 `scripts/export-kimi.ps1 -PlanOnly` 先看范围；有需求才生成ZIP。
- 最终回复以玩家能感知的结果为主，给本地或线上正确入口、是否上线、必要限制。不得把“没复现”写成“已根治”，把播放调用写成“试听通过”，把脚本过关写成“故事已好玩”。
